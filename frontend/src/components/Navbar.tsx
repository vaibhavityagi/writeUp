import Button from "./Button";

export default function Navbar() {
  return (
    <div className="flex w-full">
      <div>Saved in drafts</div>
      <button>Publish</button>
      <div>Profile</div>
    </div>
  );
}
